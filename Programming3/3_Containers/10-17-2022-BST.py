# Compare and contrast a linked list with a binary search tree / binary sorted tree (BST.)
# With a BST, instead of 1 pointer to the next, there is a right pointer and left pointer for each.
# If it's sorted properly, this drastically reduces the time it takes to retrieve something.